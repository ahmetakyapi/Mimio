/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Vercel deploys automatically detect Next.js — no special config needed.
     Add customisations below as the project grows. */
  reactStrictMode: true,
  poweredByHeader: false,
};

module.exports = nextConfig;
